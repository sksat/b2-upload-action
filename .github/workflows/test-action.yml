name: test Action

on:
  workflow_dispatch:
  push:
    paths:
      - 'action.yml'
      - 'requirements.txt'
      - 'Dockerfile'
      - '.github/workflows/test-action.yml'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - uses: ./
        with:
          key_id: hoge
          key: hoge
          bucket: hoge
          src: README.md
          dest: README.md
